{
  "name": "inventory-app-monorepo",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "start": "npm run start --workspace client",
    "start:server": "npm run start --workspace server",
    "start:client": "npm run start --workspace client",
    "build": "npm run build --workspace client"
  }
}
